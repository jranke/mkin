           error
degradation const                      
       SFO                             
       FOMC sd(log_alpha), sd(log_beta)
           error
degradation tc                                       
       SFO  sd(parent_0)                             
       FOMC sd(parent_0), sd(log_alpha), sd(log_beta)
